Goal: To have the user create a password associated with their username.

Problem: A user can be locked out of their Google account

Solution: Create a username and password on the page we ask them to pick their user name. This will require adding a textbox for the user to enter their password. Allow the user to hide / unhide the password.

Ask me questions about this implementation.

Should the password be required to complete profile setup, or optional (skippable, with a prompt to set it later)?

Required (Recommended)

How should the password be submitted -- as part of the same request that creates the profile, or a separate call?

Bundle into profile creation (Recommended)

Should there be a 'Confirm Password' field in addition to the show/hide toggle, to catch typos?

Yes, add confirm password (Recommended)

Is this only for new Google sign-ups going forward, or should existing Google-only accounts (already created with the unknown random password) also get a way to set one, e.g. in Settings?

New sign-ups only for now (Recommended)